涉及到的函数都在main_window.py中:
    ServerMainWindow.startTriggered
    ServerMainWindow.stopTriggered
    ServerMainWindow.msgAppendText
    ServerMainWindow.addConnectionItem
    ServerMainWindow.updateDataFlow

另外我在界面中还留着设置超时时间和每个用户一个共享文件夹的选项，如果你要是不需要的话，后面我就把它们去掉。